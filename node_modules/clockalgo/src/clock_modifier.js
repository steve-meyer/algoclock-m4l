const helpers = require("./helpers");


/**
 * A ClockModifier is the super class for clock algorithms that can only be used after the first algorithm
 * in a chain.
 */
class ClockModifier {

    // constructor() {}
}


module.exports = ClockModifier;
